-> RandomTour { ascend by 20 } -> TakeOff
TakeOff {
    ascend by 20
    move to point (2, 5.2133)*10
    descend by --10.21312
} -> Explore
Explore {
    ascend by --20 at speed 10
    move to point (--random[0, 100], random[0, 100])
    move to --point (3+51, 9+random[77, 100])
    move to point (--2, random[0, 100])
    descend by 10.1
    descend by 2+2
    descend by (2*3)+(3*1)
    descend by 2 + (14 - (-- 3 + 5))
} -> Investigate
Investigate {
    ascend by 20 for 10 seconds
    move by 600
    turn left by 360 at speed 10
    descend by random[0, 10.12122]
    move to point (2, random[5, 3+3])
} -> GoHome
GoHome {
    return to base
}