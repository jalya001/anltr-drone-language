package no.uio.aeroscript.type;

public enum Memory {
    VARIABLES, EXECUTIONS, REACTIONS, MESSAGES, LAMBDAS
    // Could remove Executions, Reactions, and Messages now, since they are no longer in heap
}
